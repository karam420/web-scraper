# web-scraper
Relatively simple web scraper for online stores I made using python, the elements to be parsed and selected need to be adjusted in-code but it's functional.

It searches by listing url and goes through the pages, scraping every item/product inside each page, and recursively calling the function to check the next page

Url of the listing at the site you want to search goes in the last few lines (currently line 97)
